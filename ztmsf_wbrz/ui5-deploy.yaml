# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: ztmsfwbrz
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://dev.cgdg.com:50015
          client: "500"
        app:
          name: ZTMSF_WBRZ
          description: 外部融资-利息计提台账
          package: ZTMS_LXJT
          transport: LNDK947717
        exclude:
          - /test/
